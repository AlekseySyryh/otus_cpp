language: cpp
compiler: gcc
dist: xenial

before_script:
  - sudo apt-get update
  - sudo apt-get install libdlib-dev libopenblas-dev -y
script:
  - cmake .
  - cmake --build .
  - cmake --build . --target package
deploy:
  provider: script
  skip_cleanup: true
  script:
    - sh .travis-deploy.sh